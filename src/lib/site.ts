/**
 * Site-wide constants that change more often than code.
 * BOOKING_URL: swap to the Calendly/Cal.com link when ready —
 * every "book a call" CTA on the site routes through this.
 */
export const BOOKING_URL = "/contact";
